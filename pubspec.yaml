name: photo_selector
description: 一个Flutter轻量级的照片选择器,支持iOS和Android.
version: 0.0.1
homepage: "https://github.com/xtzPioneer/photo_selector"

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  assets:
    - images/photo_selector_camera.png
    - images/photo_selector_delete.png
    - images/photo_selector_default.png